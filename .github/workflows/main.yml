name: Full CI/CD - Build Test Deploy

on:
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build & Test Next.js
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Dependencies
        run: npm ci

      - name: Build Next.js (Ignore Er
